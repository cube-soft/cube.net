<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <!--
        EntryMenu
    -->
    
    <ContextMenu x:Key="RssEntryMenu">
        <MenuItem Header="最新の情報に更新"           Command="{Binding Update}"/>
        <MenuItem Header="全ての記事を既読に設定"     Command="{Binding ReadAll}"/>
        <MenuItem Header="キャッシュをクリアして更新" Command="{Binding Reset}"/>
        <Separator/>
        <MenuItem Header="新しいフォルダーを追加"     Command="{Binding NewCategory}"/>
        <MenuItem Header="削除"                       Command="{Binding Remove}"/>
        <MenuItem Header="名前の変更"                 Command="{Binding Rename}"/>
        <Separator/>
        <MenuItem Header="プロパティ"                 Command="{Binding Property}"/>
    </ContextMenu>

    <!--
        SettingsMenu
    -->

    <ContextMenu x:Key="SettingsMenu">
        <MenuItem Header="インポート"/>
        <MenuItem Header="エクスポート"/>
        <Separator/>
        <MenuItem Header="設定"            Command="{Binding Settings}"/>
        <MenuItem Header="バージョン情報"  Command="{Binding Settings}"/>
        <Separator/>
        <MenuItem Header="終了"            Command="{Binding Close}"/>
    </ContextMenu>
</ResourceDictionary>